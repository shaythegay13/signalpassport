# Milestone 5 Verification — AI Explanation (P0b Stretch Scope)

**Date**: 2026-09-12  
**Governing PRD**: `Signal-Passport-Weekend-PRD.md` §4, §10, and §14  
**Scope**: Additive, display-only qualitative synthesis grounded strictly in immutable claims and evidence records.  
**Core Invariant**: Failure of a model call must never block Passport generation, verification, or export (PRD §4).

---

## 1. Executive Summary & Verification Matrix

Milestone 5 implements an additive, opt-in AI qualitative explanation layer for Signal Passport.
Following review feedback, all operational issues were addressed across rounds 1 and 2:
1. **Clean environment**: `.env.local` stripped of all wholesale LedgerLens variables (`NEXT_PUBLIC_SUPABASE_*`, `PRISMTRACE_*`), configured exclusively with `AI_PROVIDER=groq`, `GROQ_API_KEY`, and `ANTHROPIC_API_KEY`.
2. **Anthropic model update**: Updated Anthropic configuration from deprecated `claude-3-5-sonnet-20241022` to active model `claude-sonnet-4-5-20250929` (verified live with HTTP 200). Default provider set intentionally to Groq (`openai/gpt-oss-120b`).
3. **Date handling in invented-number check (Round 2)**: Removed date-component additions (`startDay`, `endMonth`, etc.) from `allowedNumbers` to close the hole where fabricated counts coincidentally matching date numbers (e.g. "13 contracts", "9 transfers") could pass. Grounding relies exclusively on string-based date-substring redaction for declared window dates. Added regression tests proving both fabricated cases fail while genuine date mentions pass.
4. **Coverage check for "unknown" status**: Extended coverage check to reject completeness claims for both `partial` and `unknown` coverage statuses (`coverageStatus !== "complete_for_query"`).
5. **Context budget optimization**: Capped model sample evidence at 15 records per PRD §10, avoiding Groq TPM rate limits.

| Acceptance Criterion | Description | Status | Verification Evidence |
|---|---|---|---|
| **AC 1** | Invented numbers in explanation are rejected | **PASS** | Automated test in `tests/m5-ai-explanation.test.ts` |
| **AC 2** | Nonexistent / dangling evidence IDs rejected | **PASS** | Automated test in `tests/m5-ai-explanation.test.ts` |
| **AC 3** | Coverage upgrades on partial data rejected | **PASS** | Automated test in `tests/m5-ai-explanation.test.ts` |
| **AC 3b** | Coverage upgrades on unknown data rejected | **PASS** | Automated test in `tests/m5-ai-explanation.test.ts` |
| **AC 4** | Model failure falls back to deterministic summary without throwing | **PASS** | Automated test in `tests/m5-ai-explanation.test.ts` |
| **AC 5** | `computePayloadDigest` byte-identical with/without explanation | **PASS** | Automated test in `tests/m5-ai-explanation.test.ts` |
| **AC 6** | Real provider request validated with >= 1 real evidence ID | **PASS** | Live running App One request: 2469 ms latency, `isFallback: false`, 3 verified evidence citations |
| **AC 7** | Compliance-hardening prompt pattern (constraints repeated at end) | **PASS** | Verified in `packages/analysis/src/ai/prompt.ts` |
| **AC 8** | App One displays explanation, model attribution, clean fallback | **PASS** | Verified in `apps/passport/app/page.tsx` |
| **AC 9** | App Two displays explanation clearly labeled as unverified/display-only | **PASS** | Verified in `apps/consumer/app/page.tsx` |
| **AC 10** | Comprehensive verification documentation authored | **PASS** | This document (`docs/verification/M5.md`) |

---

## 2. Provider Registry & Live Testing

### Configured Providers
The provider registry (`packages/analysis/src/ai/provider.ts`) supports pluggable backends using standard HTTP `fetch`:
1. **Groq** (`id: "groq"`): Model `openai/gpt-oss-120b` using `GROQ_API_KEY`. Configured with JSON mode (`response_format: { type: "json_object" }`) and low temperature (`0.1`).
2. **Anthropic** (`id: "anthropic"`): Model `claude-sonnet-4-5-20250929` using `ANTHROPIC_API_KEY`. Tested live: HTTP 200 SUCCESS.

### Shipped Live Request Transcript (Running App One on Port 3000)
The test was executed against the running App One server using the real EOA subject (`0xc82f8B79Cd34bD98b1abEC72475F2a73Eb15CFA8`, Ethereum Mainnet, 28 qualifying transactions):

1. **Analysis request**: `POST http://localhost:3000/api/analyze`
   - Streamed Blockscout transactions normalized and sealed into canonical payload:
     - Subject: `0xc82f8B79Cd34bD98b1abEC72475F2a73Eb15CFA8`
     - Window: `2026-08-13T00:00:00.000Z` to `2026-09-12T16:08:11.000Z`
     - Claims: 28 observed transactions, 12 active days, 10 unique recipients
     - Evidence: 28 records
     - SHA-256 Digest: `b318e8fdcc2cc487a36452391aabbf76f2c23adff4a008ea01c0a3214f7a325e`

2. **Explanation request**: `POST http://localhost:3000/api/explain`
   - HTTP Status: `200 OK`
   - Latency: `2469 ms`
   - Response payload:
```json
{
  "explanation": {
    "summary": "The address recorded 28 transactions during the observation window, across 12 active days and involving 10 distinct recipient addresses.",
    "evidenceIds": [
      "1:0xfa528e0a3f8eafb1adcbec940bd5897d2269b63fd02fbc53c0b3cd105f30c283",
      "1:0x431b27ebfc638bcea2facf6ffaa2f0c33313662837aa1a6dd20e6acae50d453c",
      "1:0x2427fb8cc5cd7eb089e16ca3e23dea3edc07bfa020352f0bf6b9a2488ea74df9"
    ],
    "model": "openai/gpt-oss-120b",
    "generatedAt": "2026-09-12T20:04:31.404Z",
    "isFallback": false
  }
}
```

3. **Validation verification**:
   - `isFallback: false` — passed all 5 strict validation checks on Attempt 1.
   - All 3 cited evidence IDs verified present in the payload evidence array.
   - Numbers in summary (`28`, `12`, `10`) match verified claims verbatim.
   - No unearned completeness claims, no causal phrases, no identity/custody claims.

---

## 3. The 5 Hard Grounding Rules & Enforcement Pipeline

All model outputs pass through `validateAiExplanation` in `packages/analysis/src/ai/validation.ts` in strict sequential order:

1. **Schema Conformance (Step 1)**:
   - Validated against `aiExplanationSchema` via Zod.
   - Requires `summary` (non-empty string <= 1000 characters) and `evidenceIds` (array of strings).
2. **Evidence Reference Resolution (Step 2)**:
   - Every cited ID in `evidenceIds` must be a member of `payload.evidence`.
   - If any evidence ID does not exist in the payload, the explanation is rejected.
3. **No Invented Numbers & Observation Window Date Substring Redaction (Step 3)**:
   - Numeric token extraction recognizes and permits observation window dates via string redaction before number extraction:
     - Exact ISO date strings (`2026-08-13`, `2026-09-12`) and timestamps are redacted from numeric checks.
     - Human-readable date formats (`August 13, 2026`, `September 12, 2026`, `13 August 2026`, `12 September 2026`) are recognized and redacted.
     - **Date components are strictly excluded from `allowedNumbers`**: Individual date numbers (`13`, `9`, `2026`) are NOT added to the allowed-numbers set. Any fabricated claim citing those numbers in non-date contexts (e.g. "13 separate smart contracts", "9 high-value transfers") is caught and rejected.
   - Any unverified numeric claims (e.g. `"$5000 volume"`, unverified transaction counts) trigger immediate rejection.
4. **Coverage Integrity (Step 4)**:
   - Extended to cover all non-complete statuses (`coverageStatus !== "complete_for_query"`), including both `"partial"` and `"unknown"`.
   - Prohibits: `"complete"`, `"exhaustive"`, `"full history"`, `"entirety"`, `"comprehensive"`, `"all transactions"`.
5. **No Identity, Ownership, or Causal Speculation (Step 5)**:
   - Banned ownership phrases: `"belong(s)? to"`, `"owned by"`, `"wallet holder is"`, `"identity is"`, `"is an entity"`.
   - Banned causal phrases: `"because"`, `"due to"`, `"in order to"`, `"caused by"`, `"as a result of"`, `"since the user"`.

---

## 4. Validation-Failure & Resilience Policy

The system implements a **fail-closed, highly resilient architecture**:
- **Why fail closed?** In a fintech verification tool, an ungrounded or hallucinated summary completely undermines trust. Displaying false numbers or hallucinated evidence references is worse than displaying a deterministic summary or no explanation.
- **1 Repair Feedback Attempt**: If validation fails on attempt 1, the pipeline reconstructs the prompt, appending:
  ```
  PREVIOUS ATTEMPT REJECTED: <exact validation error>
  CORRECT THIS SPECIFIC DEFECT. Return only valid JSON conforming strictly to the rules.
  ```
- **Deterministic Fallback**: If attempt 2 also fails, or if the model network call times out, encounters HTTP 5xx, or fails due to missing API keys, `generateExplanation` automatically calls `generateDeterministicExplanation(payload)`.
- **Deterministic Summary Format**:
  `"Observed 28 successful outgoing transactions across 12 active UTC days to 10 unique recipient addresses on Ethereum Mainnet during the declared 30-day UTC window (2026-08-13 to 2026-09-12). Coverage is complete for the declared query scope."`
  - Marked with `model: "deterministic-fallback"` and `isFallback: true`.
- **Non-blocking Invariant**: Model failures, timeouts, and JSON errors are caught. The API route (`/api/explain`) returns HTTP 200 with the fallback explanation. Generating and exporting the passport bundle never blocks.

---

## 5. Byte-Identical Digest Preservation (AC 5)

Per PRD §8 & §10, AI explanations are display metadata attached to the bundle envelope, NOT part of the canonical payload.

### Schema Diff
In `packages/schema/src/bundle.ts`:
```diff
 export const passportBundleSchema = z.object({
   schema_version: z.string().min(1),
   payload: passportPayloadSchema,
   integrity: integrityRecordSchema,
-  publication: publicationRecordSchema
+  publication: publicationRecordSchema,
+  explanation: aiExplanationSchema.optional()
 });
```

- `passportPayloadSchema` remains completely untouched.
- `computePayloadDigest(bundle.payload)` computes the SHA-256 digest over the canonical JSON serialization of `payload` only.
- In `tests/m5-ai-explanation.test.ts`, AC 5 verifies that `computePayloadDigest` produces the exact same hash whether `bundle.explanation` is present or omitted.

---

## 6. App One & App Two Integration

### App One: Generator (`apps/passport`)
- Includes an **AI Qualitative Synthesis** card below the deterministic claims.
- Dispatches `POST /api/explain` asynchronously after bundle generation.
- Displays:
  - Model badge (`openai/gpt-oss-120b` or `deterministic-fallback`).
  - Verification badge (`Grounded & Verified` vs `Deterministic Fallback`).
  - Verbatim summary and grounded evidence pills.
  - Disclaimer: *"This explanation is qualitative display text on the envelope. It does not alter or participate in the SHA-256 payload digest."*
- `handleExport` attaches `explanation` to the exported JSON bundle if generated.

### App Two: Consumer (`apps/consumer`)
- Genuinely independent offline application (zero network requests, zero analysis packages).
- When a bundle is imported:
  - Executes 4 sequential checks: Schema &rarr; Subject Address &rarr; Evidence Reference Integrity &rarr; Cryptographic SHA-256 Integrity.
  - If `bundle.explanation` is present, renders a distinct **Imported Qualitative Explanation (Display Only)** card.
  - Prominently displays:
    > **Non-Payload Display Metadata**: This qualitative explanation was attached to the envelope by the generator. It is NOT part of the canonical payload and is NOT verified by the cryptographic SHA-256 integrity check.
  - Displays cited evidence references and confirms all are present in the imported bundle's evidence list.

---

## 7. Automated Test Suite

All 63 automated tests pass cleanly (`npm test`):

```
✔ address validation: accepts valid EIP-55 checksummed mixed-case address (2.7231ms)
✔ UTC boundary: transactions near midnight UTC count as two distinct active days (8.8218ms)
✔ Task 4: generate, validate, and persist real fixture bundle to fixtures/real/passport-bundle.json (26.9899ms)
▶ canonicalJsonStringify (3.9862ms)
▶ App Two: Consumer bundle validation (PRD §9 / M3 Task 4) (22.4337ms)
▶ payload digest and integrity verification (PRD §8) (386.4137ms)
▶ bundle envelope schema (PRD §8) (28.9631ms)
✔ M4 State 1: Zero qualifying activity end-to-end pipeline (10.0438ms)
✔ M4 State 2: Invalid address pre-flight rejection (1.8208ms)
✔ M4 State 3: Provider failure invariant (never treated as zero activity) (0.3728ms)
✔ M4 State 4: Partial coverage bundle and consumer handling (0.4044ms)
✔ AC 1: A test proves an explanation containing an invented number is rejected (2.5327ms)
✔ AC 2: A test proves an explanation citing a nonexistent evidence ID is rejected (0.1571ms)
✔ AC 3: A test proves an explanation upgrading partial coverage to sound complete is rejected (0.2702ms)
✔ AC 4: A test proves simulated model failure falls back to deterministic summary without throwing (140.0434ms)
✔ AC 5: computePayloadDigest is proven unaffected by the presence or absence of explanation field (1.5003ms)
✔ M5 Banned Causal and Identity Phrases: rejected by validator (1.1034ms)
✔ M5 Valid Grounded Output: accepted by validator (2.3133ms)
✔ AC 3b: An explanation upgrading unknown coverage to sound complete is rejected (0.822ms)
✔ M5 Observation Window Dates: summary containing real window as ISO and formatted date strings validates cleanly (0.3717ms)
✔ M5 Date Component Isolation Regression: fabricated numbers matching window date components are rejected (0.3536ms)
✔ real fixture pipeline matches hand-checked values and verifies evidence integrity (12.8064ms)
ℹ tests 63
ℹ suites 4
ℹ pass 63
ℹ fail 0
```

---

## 8. Byte-Level Integrity Verification

- **BOM Check**: `0xEF 0xBB 0xBF` byte sequences searched across all files: **0 found**.
- **Control Byte Check**: Literal control bytes (`0x00..0x08`, `0x0B`, `0x0C`, `0x0E..0x1F`): **0 found**.
- **Codepage / ? Corruption Check**: Regex scanning for question mark corruption in place of section symbols: **0 found**.
- **Reference Repo Integrity**: `ledgerlens/` git working tree clean, byte-identical at commit `bd9b41c`.
